#include<stdio.h>
int main()

int sbipin;
int count = 0;

do
(
   printf("\nPlease enter atm pin number ");
   scanf("%d, &sbipin");
   count++1;
   if (usenpin == sbipin )
   (
    printf("Welcome to the SBI ATM");

   )
   else
   {
     if (count == 3)

     {
        printf("ATM CARD Block for 24 hour");
     }
     
     else
     {
        printf("\n Please enter your rimining %d times",3 count);
     }
   }

)

return0;